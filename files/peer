{% for peer in groups['nodes'] if peer != inventory_hostname %}
[Peer]
AllowedIPs = {{ hostvars[peer].wg_ip }}/32
Endpoint = {{ hostvars[peer].ansible_host }}:{{ hostvars[peer].listen_port }}

{% endfor %}
