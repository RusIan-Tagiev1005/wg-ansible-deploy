
- name: client tasks
  hosts: clients
  become: yes

  tasks:
    - name: install epel-release
      ansible.builtin.package:
        name: epel-release
        state: present

    - name: Install  htop
      ansible.builtin.package:
        name: htop
        state: present
    
